backend:
  - backend_host: "192.168.1.7"
    ports:
      - 8080
      - 8081
      - 8082


limiting:
  strategy: "no_limiting"
  rate: 1000000

load_balancing:
  strategy: "round_robin"

oxygen:
  port: 8000

health_check:
  interval_secs: 2
  check_type: "tcp"
