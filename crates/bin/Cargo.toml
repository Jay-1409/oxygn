[package]
name = "oxygen"
version = "0.1.0"
edition = "2024"

[dependencies]
types = { path = "../types" }
managers = { path = "../managers" }
network = { path = "../network" }
tokio = { version = "1.52.3", features = ["full"] }
